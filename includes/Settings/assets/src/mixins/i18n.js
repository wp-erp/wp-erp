export default {
    /* global __, sprintf */
    methods: {
        __(text, domain) {
            return __(text, domain);
        },

        sprintf(fmt, ...args) {
            return sprintf(fmt, ...args);
        }
    }
};
