/**
 * Barrel exports for the People Review feature.
 *
 * Parallel surface to `features/employees` — same underlying `erp-hr/employees`
 * store, fresh modern presentation layer for design comparison.
 */

export { PeopleReviewPage } from './PeopleReviewPage';
