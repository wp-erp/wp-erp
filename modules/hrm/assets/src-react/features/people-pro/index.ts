/**
 * Barrel exports for the People (Pro) feature.
 *
 * Enterprise-grade presentation pass on the People page — strict adherence
 * to design-system tokens (no raw hex, no playful gradients). Same
 * `erp-hr/employees` store as the other three views.
 */

export { PeopleProPage } from './PeopleProPage';
