/**
 * Barrel exports for the Employee Profile v4 layout (header card + left nav card
 * + content card). Self-contained — see EmployeeProfileV0Page.tsx.
 */

export { EmployeeProfileV0Page } from './EmployeeProfileV0Page';
