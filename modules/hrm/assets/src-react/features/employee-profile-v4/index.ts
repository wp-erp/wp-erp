/**
 * Barrel exports for the Employee Profile v4 layout (header card + left nav card
 * + content card). Self-contained — see EmployeeProfileV4Page.tsx.
 */

export { EmployeeProfileV4Page } from './EmployeeProfileV4Page';
