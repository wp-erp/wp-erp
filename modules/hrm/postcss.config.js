/**
 * PostCSS — wires Tailwind v4 via `@tailwindcss/postcss`.
 *
 * Matches Dokan's config (project.md "Reference materials" §dokan-ref).
 */

module.exports = {
	plugins: {
		'@tailwindcss/postcss': {},
	},
};
