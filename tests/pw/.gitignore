# Dependencies
node_modules/
lib/

# Local config & secrets (NEVER commit real credentials / license keys)
.env
.wp-env.override.json
# The onboarding template MUST ship — re-include it (a parent .gitignore ignores
# .env.example globally across the plugin).
!.env.example

# Auth state, wp-env data, generated run artifacts
playwright/.auth/
playwright/systemInfo.json
# Per-run timings. The COMMITTED baseline is utils/shard-durations.json, refreshed
# deliberately from aggregated CI runs — not whatever a local run last produced.
playwright/spec-durations.json
wp-data/

# Reports & test output
playwright-report/
test-results/
blob-report/
summary-report/
*.log

# Internal QA docs (kept local, not committed to the public repo)
PLAN.md
test-plans/
bug-reports/

# Editor / OS / TS build
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
npm-debug.log*
yarn-error.log
